#include<bits/stdc++.h>
using namespace std;

int main(){

    int num1, num2;
    cin >> num1 >> num2;
    // inbuilt functions
    int minimum = min(num1, num2);
    cout << minimum;
    int maximum = max(num1, num2);
    cout << maximum;

    return 0;
}